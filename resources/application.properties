
 #server.port=8082
 #server.address=137.184.69.10

  server.port=8082
   server.address=localhost
  # server.address=10.111.173.76

app.cors.allowed-origin=http://localhost:8085

spring.datasource.url=jdbc:mysql://localhost:3306/car_dealer
spring.datasource.username=iotmanager
spring.datasource.password=Alfra-0922
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect


# Allow Tomcat to swallow the entire failed upload to invoke your error handler
server.tomcat.max-swallow-size=-1
# Increase the overall limit for POST requests
server.tomcat.max-http-post-size=64000MB
spring.servlet.multipart.max-file-size=64000MB
spring.servlet.multipart.max-request-size=6400MB


# Upload directory
upload.path=./uploads

file.upload-dir=./uploads

spring.web.resources.static-locations=file:${upload.path}

# logging.file=/usr/iot/spring-boot-app.log
logging.file.path=/usr/iot/logs

#upload files configurations
spring.servlet.multipart.enabled=true 

#spring.jpa.show-sql:true

